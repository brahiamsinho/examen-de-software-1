# Tasks: Carry Generation Profile Metadata Through Relational Mapping

Strict TDD: every implementation task is preceded by a RED task (write the test, run it, see it fail for the expected reason). One test module per step. All commands run in Docker: `docker compose exec -T backend pytest -q <path>`.
Base path `B = backend/apps/relational_mapping`. Test files live in `B/tests/`.
Spec refs: `GP` = `specs/generation-profile/spec.md`, `RM` = `specs/relational-mapping/spec.md`. Design refs: DD132-DD141 in `design.md`.
Expected size: ~300-350 authored lines, single PR, no parallel work (each step feeds the next; one writer).

## Phase 1: Profile value objects (DD132, DD133, DD134)

- [x] 1.1 RED - Create `B/tests/test_profile.py`. Cover: `ColumnProfile()` / `TableProfile()` all fields `None`; `FrozenInstanceError` on assignment for both; equal separately built `TableProfile` (with `DefaultSort` + `crud` tuple) are `==`, hash equal, `{p1} == {p2}`; `SortDirection`/`CrudOperation` values (`asc`/`desc`, `create`/`read`/`update`/`delete`). Run: fails with `ModuleNotFoundError` (`domain.profile` missing). Refs: GP "Profile Value Objects".
- [x] 1.2 GREEN - Create `B/domain/profile.py` exactly as in design "Interfaces": `SortDirection`, `CrudOperation`, `_CRUD_ORDER`, `DefaultSort(attribute_id, direction)`, `ColumnProfile(searchable, sortable, read_only)`, `TableProfile(entity, auditable, read_only, crud, default_sort)`; all `X | None = None`, `crud: tuple[CrudOperation, ...] | None`. Run `pytest -q apps/relational_mapping/tests/test_profile.py`: green.

## Phase 2: Error class (DD136)

- [x] 2.1 RED - Create `B/tests/test_profile_parser.py` with only the error tests: `InvalidGenerationProfileError` with `key=None` -> `str(exc) == "Invalid generation profile for element 'e1': metadata entry is not a mapping"`; with key -> `"Invalid generation profile for element 'a1', key 'searchable': expected a boolean"`; `exc.element_id`, `exc.key`, `exc.reason` set; `issubclass(..., UnmappableModelError)` and catchable as it. Fails with `ImportError`. Refs: GP scenarios "Error message with a key", "Error is an UnmappableModelError", Rule 1 message.
- [x] 2.2 GREEN - Add `InvalidGenerationProfileError(UnmappableModelError)` to `B/mapping/errors.py` per design (ctor `element_id, reason, key=None`; two message formats using `{element_id!r}` / `{key!r}`). Run the file: green.

## Phase 3: Strict parser (DD135, DD137, DD138, DD139, DD140; rules 1-12)

- [x] 3.1 RED - In `B/tests/test_profile_parser.py` add table-driven happy-path tests via `parse_table_profile(element_id, entry)` / `parse_column_profile(element_id, entry)`: full table entry (`crud ["read","create"]` -> `(CREATE, READ)`, `defaultSort` -> `DefaultSort(ElementId("attr-total"), SortDirection.DESC)`); full column entry; partial entries (other fields `None`, not `False`); `crud` reorder (`["delete","read","create"]` == `["create","read","delete"]`, equal hashes); `crud: []` -> `()` (not `None`); `{"profile": {}}`, `{}`, and `{"source": "llm", "confidence": 0.9}` -> `None` at both levels; provenance keys ignored even when malformed (`{"source": "llm", "confidence": "not-a-number", "aliases": 7, "profile": {"auditable": True}}`); `entity: False` stored; unresolvable `defaultSort.attribute` accepted; same entry parsed twice -> `==` and equal hash. Fails with `ImportError` (`profile_parser` missing). Refs: GP "Key Vocabulary and Levels", "None Means Undeclared", "CRUD Canonical Ordering", "defaultSort Is Stored Unresolved", "Entity Key", "Keys Outside profile", "Parser Purity and Determinism".
- [x] 3.2 RED - In the same file add `@pytest.mark.parametrize` error tests asserting `InvalidGenerationProfileError` plus exact `key`, `reason` and `str(exc)`, one group per rule: R1 list entry; R2 `{"profile": ["searchable"]}`; R3 unknown key at each level (`colour`), cross-level (`searchable` on class, `crud` on attribute), and `aliases`/`required`/`unique` inside profile; R4 values `1`, `0`, `"true"`, `None` for `searchable` (column) and `auditable` (table); R5 `"read"`, `{"read": True}`, `["read", 1]`; R6 `["read","READ"]` -> `unknown CRUD operation 'READ'`; R7 `["read","create","read"]` -> `duplicate CRUD operation 'read'`; R8 `defaultSort: "attr-total"`; R9 missing `attribute` / missing `direction`; R10 `"nulls"` -> key `defaultSort.nulls`; R11 `""`, `5`, `None`; R12 `"ASC"`, `"up"`, `None`. Use single-fault inputs only (within-entry rule ordering is unspecified, see Cross-check finding C). Fails with `ImportError`.
- [x] 3.3 GREEN - Create `B/mapping/profile_parser.py` with `parse_table_profile` and `parse_column_profile` implementing rules 1-12 with the exact reason strings; strict `isinstance(v, bool)`; exact-value CRUD membership; `crud` canonicalized via `_CRUD_ORDER`; zero declared keys or absent `"profile"` -> `None`; imports only `domain.profile`, `mapping.errors`, `apps.uml_modeling.domain.ids.ElementId`. Run `pytest -q apps/relational_mapping/tests/test_profile_parser.py` and `test_profile.py`: green.

## Phase 4: Schema fields (RM "RelationalModel Domain Structure")

- [x] 4.1 RED - In `B/tests/test_schema.py` add `profile is None` assertions to `test_column_defaults` (~`:49`) and `test_table_defaults` (~`:94`); add tests that `hash(Column(..., profile=ColumnProfile(searchable=True)))` and `hash(Table(..., profile=TableProfile(auditable=True)))` succeed and equal separately built values compare `==` with equal hashes. Fails: `TypeError`/`AttributeError` (no `profile` field).
- [x] 4.2 GREEN - In `B/domain/schema.py` append `profile: ColumnProfile | None = None` as the last field of `Column` and `profile: TableProfile | None = None` as the last field of `Table`; import from `domain.profile`. Run `pytest -q apps/relational_mapping`: entire existing suite plus new tests green.

## Phase 5: Mapper wiring (DD141, STI and synthetic columns)

- [x] 5.1 RED - Create `B/tests/test_map_profiles.py` using `tests/factories.py` with hand-built `generation_metadata`. Cover (RM refs in brackets): declared class profile on `Table` (`TableProfile(auditable=True, crud=(READ,))`) [Class-to-Table]; declared attribute profile on `Column` incl. enum-typed attribute [Attribute-to-Column]; undeclared -> `None` for both; `entity: False` still produces the table with `Table.profile.entity is False`; STI root wins (`read_only is None`); subclass-only class entry -> `Table.profile is None`, no error; malformed subclass class entry (`auditable: 1`) raises for the `Car` id with key `auditable`; subclass-owned attribute keeps profile and `owning_class_id` is the subclass id; `id`, `class_type`, FK, join-table `id` and join FK columns all `profile is None` even when owner declares profiles, join `Table.profile is None` [Synthetic Columns]; unknown element ids (value `"garbage"`) and malformed keys outside `"profile"` never raise [Collected Before Table Work]; malformed attribute profile `{"searchable": "yes"}` raises before any `RelationalModel` is returned, caught as `UnmappableModelError`; two malformed entries `A` then `B` -> both calls raise for `A` with identical messages. Fails: profiles absent from output / no error raised.
- [x] 5.2 GREEN - In `B/mapping/mapper.py`: add imports (`ColumnProfile`, `TableProfile`, `parse_column_profile`, `parse_table_profile`, `InvalidGenerationProfileError`); add `_collect_profiles(model, class_by_id, attribute_owner_by_id)` returning `(table_profile_by_id, column_profile_by_id)`, iterating `model.generation_metadata` in its own order, dispatching class id / attribute id / skip; call it in `map_to_relational` right after `class_by_id` is built and before `_map_enumerations`. Run 5.1 tests: malformed-abort, first-wins, unknown-id tests green.
- [x] 5.3 GREEN - Column wiring: add keyword-only `profile: ColumnProfile | None = None` to `_map_attribute_column`, pass `profile=profile` in BOTH `Column(...)` returns (enum branch and primitive branch); in `_map_table_for_root` pass `profile=column_profile_by_id.get(attribute.id)`; add `table_profile_by_id` / `column_profile_by_id` keyword-only params and thread them from `map_to_relational`. Run 5.1: attribute/enum/subclass-owned/synthetic-column tests green.
- [x] 5.4 GREEN - Table wiring: add `profile: TableProfile | None = None` as last field of `_TableDraft`; set `draft.profile = table_profile_by_id.get(root_id)` right after `_TableDraft(...)` in `_map_table_for_root`; pass `profile=draft.profile` in `_freeze_table`'s `Table(...)`. Run 5.1 fully and `pytest -q apps/relational_mapping`: green.
- [x] 5.5 TEST - In `B/tests/test_determinism.py` add: a model whose classes and attributes declare profiles maps twice to `==` results with equal `hash()` of tables and columns [RM "Deterministic Mapping"]. Note: passes immediately (determinism holds by construction); it is a characterization test protected by mutation check 7.3/7.4 below, not a RED step.

## Phase 6: Purity / import guard (GP "Module import purity", DD135)

- [x] 6.1 TEST - In `B/tests/test_profile.py` add an `ast`-based guard reusing the `_imported_module_names` helper pattern from `test_schema.py:30-38` and `_FORBIDDEN_IMPORT_PREFIXES` (`django`, `psycopg`, `sqlite3`, `MySQLdb`, `java`, `org.springframework`): `domain.profile` and `mapping.profile_parser` import none of them; `profile_parser` additionally imports none of `apps.uml_modeling.domain.model`, `apps.domain_manifest`, `apps.spring_generator`. Written after 3.3 so it passes immediately; prove it can fail by temporarily adding `import django` to `profile.py` and `from apps.uml_modeling.domain.model import CanonicalUmlModel` to `profile_parser.py`, confirming a red run, then reverting.

## Phase 7: Mutation checks (design "Mutation-check candidates"; apply, run named test, confirm RED, revert)

- [x] 7.1 Mutation 1: replace `isinstance(v, bool)` with truthiness in the parser; `test_profile_parser.py` rule-4 cases (`{"searchable": 1}`) must fail. Revert.
- [x] 7.2 Mutation 2: change STI root lookup (`root_id`) to any-class-in-tree; `test_map_profiles.py` subclass-only-entry and root-wins tests must fail. Revert.
- [x] 7.3 Mutation 3: return an empty `TableProfile()` / `ColumnProfile()` instead of `None` for zero declared keys; parser `{}` / missing / provenance-only tests and mapper undeclared tests must fail. Revert.
- [x] 7.4 Mutation 4: drop `_CRUD_ORDER` canonicalization; the `["read","create"]` -> `(CREATE, READ)` and reorder-equality tests must fail. Revert.
- [x] 7.5 Mutation 5: pass a profile into a synthetic column (`id`, `class_type`, FK, or join-table column); the synthetic-column tests in `test_map_profiles.py` must fail. Revert.
- [x] 7.6 Mutation 6: invert strictness (raise on keys outside `"profile"` / ignore unknown keys inside); provenance-ignored and rule-3 tests must fail. Revert.
- [x] 7.7 Mutation 7: shorten the error message (drop `element_id` or `key`); exact `str(exc)` assertions in 2.1 / 3.2 must fail. Revert. Then rerun `pytest -q apps/relational_mapping` to confirm the tree is green again.

## Phase 8: Output-neutrality and verification (RM "Profile Carry-Through Is Output-Neutral")

- [x] 8.1 TEST - Add the test for RM scenario "Declared profile does not change manifest or oracle output": build the sample model twice, once with declared profiles in `generation_metadata` and once without, and assert identical Domain Manifest and Spring project output. Design "File Changes" does not list a file for this scenario (Cross-check finding B); proposed location: new `backend/apps/generation_runner/tests/test_profile_output_neutral.py` (the runner already composes mapper, manifest and Spring generator). Confirm location with the caller before writing; test only, no source edits in `domain_manifest` / `spring_generator`.
- [x] 8.2 VERIFY - `docker compose exec -T backend pytest -q apps/relational_mapping`: green.
- [x] 8.3 VERIFY - `docker compose exec -T backend pytest -q apps/generation_runner/tests/test_sample_model.py` (`EXPECTED_FILE_COUNT = 41`, determinism): green with no edited expectations.
- [x] 8.4 VERIFY - `docker compose exec -T backend pytest -q apps/spring_generator/tests/test_inheritance_backward_compatibility.py` (per-file sha256 goldens): green with no edited expectations.
- [x] 8.5 VERIFY - `docker compose exec -T backend pytest -q apps/domain_manifest` (`test_manifest.py::test_undeclared_facts_are_never_emitted` over `EXCLUDED_KEYS` still containing `aliases`, `test_determinism.py`): green with no edited expectations.
- [x] 8.6 VERIFY - `docker compose exec -T backend pytest -q` (full backend suite): green.
- [x] 8.7 VERIFY - `git diff --stat` shows no change to non-test sources of `backend/apps/domain_manifest/`, `backend/apps/spring_generator/`, `backend/apps/uml_modeling/domain/model.py`, and no edits to the four oracle test files or their expected values; diff size within ~300-350 authored lines.

## Phase 9: Project memory docs (AGENTS.md mandatory rule)

- [x] 9.1 `docs/ai/DECISIONS_LOG.md`: append DD132-DD141 (one line each, matching design titles: DD132 profile module location, DD133 tri-state `None`, DD134 hashability/crud tuple, DD135 model-free pure parser, DD136 error subclass of `UnmappableModelError`, DD137 disjoint key levels except `readOnly`, DD138 `aliases`/`required`/`unique`/provenance out, DD139 `entity` carried not acted on, DD140 `defaultSort.attribute` unresolved `ElementId`, DD141 profiles collected up front). Existing log ends at DD130 and states no DD132+ existed, so numbering is free.
- [x] 9.2 `docs/ai/CURRENT_STATE.md`: record that the mapper now carries `Column.profile` / `Table.profile`, no consumer reads it, oracle and manifest byte-identical, new test counts from 8.6.
- [x] 9.3 `docs/ai/HANDOFF_LATEST.md`: overwrite with this change's handoff (files touched, verification results, deferred items: `required`/`unique`, `entity: false` semantics, `defaultSort` resolution, authoring path).
- [x] 9.4 `docs/ai/NEXT_STEPS.md`: add slice 2 (Domain Manifest emission of profile fields, unblocked DD131) and slice 3 (Spring filtering/search/sorting); note the authoring path is still missing.
- [x] 9.5 Create `docs/ai/sessions/2026-09-20-<agent>-relational-generation-metadata.md` summarizing the session (decisions, tests, gate results).
